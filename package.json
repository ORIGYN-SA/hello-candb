{
  "name": "hello-candb",
  "version": "0.0.0-alpha.7",
  "description": "",
  "scripts": {
    "generate-actor": "node tools/generate_actor.js",
    "generate-declarations": "dfx generate",
    "copy-declarations": "rm -rf frontend/declarations && cp -r src/declarations frontend/declarations",
    "refresh-declarations": "npm run generate-declarations && npm run copy-declarations"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "generate-template-files": "^3.2.1"
  }
}
